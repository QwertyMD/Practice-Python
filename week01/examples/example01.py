length = 10  # Assigns the value 10 to the variable `length`
width = 5  # Assigns the value 5 to the variable `width`
area = length * width  # Calculates the area by multiplying `length` and `width`, and assigns the result to the variable `area`
print(area)  # Prints the value of `area` to the console