def greeting(name):  # Defines a function named `greeting` that takes one parameter `name`
    print("Welcome " + name)  # Prints a welcome message concatenated with the value of `name`

greeting("RaviKumar")  # Calls the `greeting` function with the argument "RaviKumar"
