base = 6  # Assign the value 6 to the variable 'base'
height = 2  # Assign the value 2 to the variable 'height'
area = base * height / 2  # Calculate the area of the triangle using the formula (base * height) / 2
print("Area is " + str(area))  # Print the area of the triangle, converting the area to a string for concatenation
