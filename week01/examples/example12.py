x = 1000  # Assign the value 1000 to the variable 'x'
while x != 0 and x % 2 == 0:  # Loop while x is not 0 and x is even
    x = x / 2  # Divide x by 2
    print(x)  # Print the current value of x